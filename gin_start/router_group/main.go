package main

func main() {

	/*router := gin.Default()

	router.GET("/goods/list", goodList)
	router.GET("/goods/add", goodAdd)
	router.GET("/goods/delete", goodList)
	router.GET("/goods/detail", goodDetail)

	router.GET("/user/edit", userEdit)
	router.GET("/user/login", userLogin)
	router.GET("/user/exit", userExit)

	//可以看到我们可以对路由进行分组，这样可以更好的管理路由
	//比如我们可以将所有的商品相关的路由放在一个组里面，将所有的用户相关的路由放在一个组里面
	goodsGroup := router.Group("/goods")
	{
		goodsGroup.GET("/goods/list", goodList)
		goodsGroup.GET("/goods/add", goodAdd)
		goodsGroup.GET("/goods/delete", goodList)
		goodsGroup.GET("/goods/detail", goodDetail)
	}

	userGroup := router.Group("/user")
	{
		userGroup.GET("/user/edit", userEdit)
		userGroup.GET("/user/login", userLogin)
		userGroup.GET("/user/exit", userExit)
	}

	router.Run(":8082")*/
}
